{
  "name": "otpinput",
  "version": "1.0.0",
  "description": "otp input with seperator and placeolder",
  "main": "index.js",
  "scripts": {
    "start": "http-server ./src -a localhost -p 3000 -c-1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http-server": "^0.11.1",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "css-loader": "^1.0.0"
  }
}
